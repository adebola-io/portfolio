export default function NotFound () {
    return <main id='not_found' className="page">
        <div className="not_found_stage">
            <span>ERROR: Page not found.</span>
        </div>
    </main>
}